$("#convolution")
  .html("<%= escape_javascript(
    render  partial: "result",
            locals: {
              operation: "Convolution",
              intervals: @intervals,
              plot_data: @convolution
            }
  )%>");

$("#correlation")
    .html("<%= escape_javascript(
    render  partial: "result",
            locals: {
              operation: "Correlation",
              intervals: @intervals,
              plot_data: @correlation
            }
  )%>");
