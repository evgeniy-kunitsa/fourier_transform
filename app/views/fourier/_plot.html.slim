h2 #{plot_title}
= render  partial: 'plot_canvas_and_data',
          locals: { \
             plot_id: plot_id,
             chart_type: chart_type,
             values: { \
               x: values[:x],
               y: values[:y] \
             } \
          }
